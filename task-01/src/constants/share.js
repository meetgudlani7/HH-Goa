export const CAPTION_PLACEHOLDER =
  'The card is ready.\nThe builder is accounted for. ⚡\n\nNow we just need to see what happens in Goa.\n\n#FrameInGoa #HackerHouseGoa';

// Below this viewport width, "Post to X" uses the OS share sheet (auto-
// attaches both images, user picks X from the sheet). At/above it — laptop
// and desktop screens — it skips the sheet and goes straight to X with
// both images downloaded for manual attach instead. 768px is the standard
// phone/tablet breakpoint; adjust here if "starting at laptops" should mean
// a different width.
export const MOBILE_BREAKPOINT_PX = 768;
